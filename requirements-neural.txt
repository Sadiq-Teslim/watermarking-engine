# Full neural tier (image + audio + video). Heavy; installed only when INSTALL_NEURAL_FULL=true.
# CPU torch builds; swap the index URL for a CUDA build on a GPU host.
-r requirements-neural-image.txt
torchaudio==2.5.1
audioseal==0.1.4
soundfile==0.12.1
# Video neural tier (P7). Pin a specific commit at deploy for reproducibility.
videoseal @ git+https://github.com/facebookresearch/videoseal.git
