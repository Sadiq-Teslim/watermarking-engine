# Optional neural tier (P6 audio / P7 video). Heavy — installed only when INSTALL_NEURAL=true.
# CPU torch builds; swap the index URL for a CUDA build on a GPU host.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1
audioseal==0.1.4
soundfile==0.12.1
# Neural image tier (image tool). Pin/validate against the installed version at deploy.
pillow==11.0.0
trustmark==0.7.0
# Video neural tier (P7). Pin a specific commit at deploy for reproducibility.
videoseal @ git+https://github.com/facebookresearch/videoseal.git
